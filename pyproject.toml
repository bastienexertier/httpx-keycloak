[project]
name = "httpx-oauth2"
version = "1.0.7"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Bastien Exertier", email = "exertier.bastien@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "jwt>=1.3.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
